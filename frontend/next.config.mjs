
//  @type {import('next').NextConfig} 
//

const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "img.clerk.com",
      "images.remotePatterns",
    ],
  },
};

export default nextConfig;
